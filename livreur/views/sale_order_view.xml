<odoo>
    <record id="view_order_tree_livreur" model="ir.ui.view">
        <field name="name">sale.order.tree.livreur</field>
        <field name="model">sale.order</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="date_order"/>
                <field name="state"/>
                <field name="amount_total"/>
            </list>
        </field>
    </record>

    <record id="action_order_livreur" model="ir.actions.act_window">
        <field name="name">Bons de commande</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">list,form</field>
        <field name="context">{}</field>
        <field name="domain">[]</field>
    </record>

    <menuitem id="menu_livreur_root"
              name="Livraisons"
              web_icon="sale,static/description/icon.png"
              sequence="20"
              groups="livreur.group_livreur"/>

    <menuitem id="menu_order_livreur"
              name="Commandes à livrer"
              parent="menu_livreur_root"
              action="action_order_livreur"
              groups="livreur.group_livreur"/>
</odoo>